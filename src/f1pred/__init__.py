"""f1pred — minimal helpers for the 2026 F1 race-result prediction project.

Real work happens in notebooks/. This package holds only the small, reusable
pieces the notebooks import (paths, data loading, features, model, evaluation).
"""

__version__ = "0.1.0"
